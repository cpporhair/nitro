# Inconel — Known Issues

> 已确认存在 + 已确认延后的问题登记。一行一条，方向一句话。
> 真正动手修时回 audit / spec / 代码再研究；本文不是实施手册。

## Priority

- `urgent`  不被 dependency 阻塞，尽快处理
- `blocked` dependency 锁住，等解封条件
- `normal`  标准 backlog
- `low`     改善向

## Open

> 按“实现前提”分组，而不是按 priority 排序。优先级仍看 `Priority` 列；分组只回答“现在做是否会顺、会不会返工、在什么模块边界上做最合适”。

### 现在就可以实现：低依赖清理 / 命名 / 文档

这些项不需要等新模块落地，主要是局部清理、命名对齐或文档化。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|

### 现在就可以实现：当前运行时路径改进

这些项都建立在当前已有 tree/value/runtime 路径上，不依赖 future 模块，属于现在就能直接收敛的行为或结构问题。

> 2026-06-02 决策：`INC-054` 先作为 deferred TODO 保留，不阻塞整体模块推进。当前旧 watermark 协议热路径最快且能避免 silent overlap；是否投入 push floor / ENOSPC backpressure 方案，等 `coord/front/wal/pipeline/recovery` 主路径完成后，用实际生产 workload 数据观察 tree/value 边界压力、reclaim 后低位连续 free 是否足够、撞墙频率和容量利用率，再重新决策。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-054 | INC-051 落地时 manager 端 `sync_alloc_floor(...)` / `published_alloc_floor_lba` 路径已就绪（037 plan §"地址空间"），但 tree 侧 `apps/inconel/tree/owner_scheduler.hh::tree_allocator::allocate`（L68-97）仍是 v6 简化协议：relaxed-load `value_head_lba`，`if (next_end_lba > value_low) panic`，从未调用 `sync_alloc_floor`、从未发布 floor reservation。**当前不触发 silent corruption**（v6 的 tree_head 单调上升 + value_low_watermark 单调下降两条不变量挡住 stale read），但稳态下两个问题：(1) value 累积的低位连续 free 永远不被 tree 复用——`value_low_watermark` 只在新 carve 拉低时下降、reclaim 不上升，tree 没有反向接管路径；(2) 撞墙 panic 是错误的失败模式——真撞墙时盘可能只用 30%（活值散布在 [hi, end) 内、free 在中间夹着），把"碎片化让不出"误升级为"进程崩溃"，应是 graceful ENOSPC backpressure。Issue 独立，可任意时刻启动。 | 2026-04-25 INC-051 接入完成 review 顺出；2026-04-27 设计 review 推翻原"纯 pull (sync_alloc_floor 异步协议)"方向；现状 `apps/inconel/tree/owner_scheduler.hh:68-97` `tree_allocator::allocate` | `urgent` | **方向（2026-04-27 review 修订）**：push fast path + tree 撞墙 ENOSPC backpressure（不 panic、不走纯 pull 异步协议、不走统一分配）。改造：(1) value 端加 `try_advance_acked_floor()` helper：在 `release_values` + `global_free_extents_` coalesce 后从 free 列表找贴 acked_floor 上端的连续段，翻 5 本账（partial / inflight / trim / cached / global_free，复用 `sync_alloc_floor` 内部 validate helper）确认全空 → store-release 发布 `acked_alloc_floor_lba` 到 atomic（建议落在 `core::data_area_heads`）；(2) tree 端 `tree_allocator::allocate` 改为 acquire-load acked_floor + check next_end，撞墙返回 `unsupported_alloc` 给 caller，merge round 接到后走 ENOSPC backpressure 而非 panic；(3) 删 v6 的 `value_head_lba.load + panic` 检查 + 从 `core::data_area_heads` 移除 `value_head_lba` 字段（不再有 cross-core 语义；value 内部 `value_low_watermark_lba_` 可保留为 owner-local debug counter）。**否决方案（防再选）**：(a) 纯 pull (sync_alloc_floor 异步协议) —— 工程上要新 PUMP sender + op_pusher + tree allocate 异步化 + merge 协程 yield，~几百行 + 协程改造；性能上 RTT batch 摊销仍 ~20 ns/allocate（~2% 写入热路径）；唯一优势"协议状态机单调"不抵实现复杂度。(b) 统一分配 (tree+value 都放 value owner core) —— 解决碎片化达 ~100% 容量利用，但 single-core ~100 万 allocate/sec 卡死整体 throughput + 破坏 share-nothing 多核架构，吞吐瓶颈无法通过加机器弥补。**关键判断**：撞墙 ≠ 盘满（碎片化下盘可能只用 30% 也撞墙，必须 graceful ENOSPC 不能 panic）；碎片化两边都不解决（push 和 pull 在功能上等价，都只让贴墙连续段、都不迁移数据）；carve_high 策略让活值集中高位 → 低位累积大段连续 free 是稳态行为，push 让墙覆盖这部分大头，真碎片化走 ENOSPC backpressure 是 graceful failure。**测试**：push 让墙正确性（reclaim 后 acked_floor 推进 / 不贴墙不动 / 5 本账任一占据则不让）+ tree 撞墙返回 unsupported_alloc 非 panic + carve_high 长跑下让墙累积有效性（容量利用率 vs workload）+ 跨 core fence ordering（store-release / acquire-load 不 stale）+ ENOSPC backpressure 集成（业务层降级，DELETE 仍可执行让 reclaim 推进）。 |
| INC-056 | value persist 的 nonresident_partial prefill **owner 链路**缺单元级直接测试：`handle_persist` 产出 `persist_prefill` → pipeline bounded reads → `continue_persist` 重入 → leader writes，以及 prefill 读失败时的 follower `ok=false` 通知 + `rollback_published_round`，这条链当前只靠真盘 `inconel_test_flush_e2e` 大量级 + reclaim 间接触发（INC-051 接入时的 3 个 prefill bug 即由此暴露）。M07/046 的 `m07_bounded_prefill_read_inflight` 只覆盖 `nvme::read_frame_range_bounded` helper 本身（bounded 机制正确性），未触达 owner 链路。构造路径存在但较深：小 data_area 逼 pressure mode + persist 满页 → reclaim 部分 slot → 逐出 resident/cached frame → 同 class 再 persist 拿 `nonresident_partial` claim。 | 2026-06-12 M07/046 review 顺出（实现侧把 046 §9.6 降级为 helper 级且总报告未产出，review 独立发现；见 046 §9.6 review 记录） | `normal` | 在 m07 测试基建（instrumented fake NVMe + `value_fixture`）上补 owner 级 prefill 测试：成功路径断言 prefill reads 并发 ≤ `max_read_inflight`、round commit、`out_vr` 正确且可 `read_value` 读回；失败路径（fake 注入 read false）断言 leader 收到异常、follower 收到 `persist_follower{ok=false}`、rollback 后下一 round 正常 persist。fixture 需要小 `data_area` + 小 `cache_capacity` + 显式 reclaim 构造 |
| INC-047 | 当前 canonical leaf-only merge 对 `delete-to-empty` 仍采用保守 empty-leaf 表示，尚未支持 `zero-child delta -> prune -> internal/root collapse` 的完整结构收缩链。现状是 worker 在 leaf merge 结果为空时仍产出一张 0-record empty leaf，owner 也明确不做 prune/collapse；因此 lookup / 后续 flush 仍正确，但 tree shape、`leaf_order` 和 `reverse_topology` 会保留本可删除的 empty leaf / 可折叠 internal，属于 correctness-safe 的结构收敛缺口而不是 `INC-046` 那类跨 round correctness bug。 | 2026-04-19 `INC-046` 实现 review 顺出；`apps/inconel/tree/candidate_build.hh::merge_and_build_leaf_impl` 空结果仍 emit empty leaf，`apps/inconel/tree/owner_scheduler.hh::run_leaf_only_merge` 明确 “does not implement zero-child delete pruning or internal collapse”，且 `build_leaf_order_from_replacements` / `build_reverse_topology_from_overrides` 仍按 replacement/override 模型重建 manifest | `normal` | 把 `delete-to-empty` 单独建模成 `0-child child_interface_delta`：owner reverse 支持 child removal、single-child internal collapse、root-empty/root-single-child 收敛，并在最终树形上统一重导 `leaf_order` / `reverse_topology` / `slot_map`；测试至少覆盖 leaf 删空、parent 由 2 child 变 1 child、multi-level collapse、root 收敛为空树 |
| INC-049 | Owner merge 协程的 CPU 让出分支是 dead code：`apps/inconel/tree/owner_scheduler.hh:131` 的 `enum merge_yield { need_io, yield_cpu, done }` 声明了 `yield_cpu` 语义（注释："no IO, but CPU has been on-shift too long; let main loop breathe"），`build_merge_step_decision_after_yield` 的 `case merge_yield::yield_cpu:`（line 2300-2305）也实现了对应处理（返回空 IO batch 让 outer pipeline 空转一圈 drain 其它 scheduler），但 `run_leaf_only_merge` 协程 body 里**没有任何 `co_yield merge_yield::yield_cpu` 的 emit 点**——所有 yield 站点（1772 / 1776 / 1925 / 2103 / 2107）都是 `need_io`，结尾（1800 / 2144）是 `done`。后果：当 owner 在某个 level / phase 产生连续 CPU 工作但不伴随 IO emit 时（例如未来 `build_internal_pages_owner` 对高层大合并层扫描 / 重 format），tree_sched 会被 CPU 段锁住，既不给其它 submit 让路，也不让 NVMe 在这段 CPU 期间之后"立刻"被再次喂 IO（只能等到下一个 `co_yield need_io` 批次），可能出现 NVMe 空跑 / tree_sched 饿其它请求的情况。现阶段因为 coroutine 每次都是"staging 一批 writes → yield need_io → 再 staging 下一批"的紧凑节奏（实测 writes/iter ≈ 30 稳定），不触发；scale 放大到真 NVMe / owner 跨 level 处理变重时可能就会 | 2026-04-21 step 035 Phase 1 owner merge observability 实测（merge_iter_count / owner_cache_hits 等 `_flush_debug` 计数）review 顺出 | `low` | 给 owner merge 协程加 "CPU budget" 触发点：在 `run_leaf_only_merge` 的关键 CPU 密集段（如 `build_internal_pages_owner` 外层循环、level transition 的 parent splice 大循环）里维护一个自 last yield 起的 tick / chunk 计数器，越过阈值就 `co_yield merge_yield::yield_cpu`；阈值以"tree_sched advance 一个 tick 的预算"为单位（或最保守：N 个 parent 处理后强制让一次）。现有 handler 不改，只需要填 emit 点。归类为 low 是因为目前没有实测触发，属于"等 scale / 真 NVMe 上线才会暴露"的 fairness 潜在坑 |
| INC-050 | Tree page CRC 是 owner 合并阶段的单一最大 CPU 项，远低于硬件 CRC32C 应有吞吐，且全部集中在 owner 单核算。Step 035 Phase 1 N=1M 跑 `perf record -e cycles:u` 实测：flush#1 owner 线程 70% 时间在算 CRC（`libabsl_crc_internal` 55% + `CRC32::Extend` 6%），折算绝对时间 ~109ms / 267ms 单轮 flush；总 CRC 数据 10 377 pages × 4 KiB = 40.5 MiB，吞吐仅 **371 MiB/s**，距 x86 `_mm_crc32_u64` 硬件 CRC32C 单核 3–10 GB/s 差 ~25×。根因三条：(1) absl 是**动态链接**的 `libabsl_crc_internal.so.2601.0.0`（18 KB，全 PLT 跳转，跨 DSO 无法 inline）—— `ldd` 可见；(2) `apps/inconel/format/tree_page.hh::tree_page_compute_crc`（line 156-167）**每张 page 拆两次调用**（`absl::ComputeCrc32c` + `absl::ExtendCrc32c` 绕过 `page_crc` 字段），flush#1 = 20 754 次跨 DSO 跳转；(3) 架构上 **workers 不算 leaf page CRC**，所有 tree page（包括 worker 产出的 leaf）最终都由 owner 单核串行 CRC，没有并行分摊 | 2026-04-21 step 035 Phase 1 perf 观测；调用点：`format/tree_page.hh:160,162` / `format/value_object.hh:56,86` / `format/wal.hh:131,138,346,384,461` / `format/superblock.hh:97` | `urgent` | 优先做 B（代码级，低风险）：新增 `apps/inconel/format/crc32c.hh` 提供 inline 硬件 CRC32C（`_mm_crc32_u64` / `__builtin_ia32_crc32di`，header-only，`-O3 -flto` 能 inline 到每个 call site），替换掉 tree_page / value_object / wal / superblock 四处的 `absl::ComputeCrc32c` 调用，并把 `tree_page_compute_crc` 的"绕过 4 字节 CRC 字段"逻辑改成一次 CRC 加清零字段上下文的小 helper（或者单次线性 CRC 后 XOR 掉那 4 字节对 CRC32C 的贡献）；预期 CRC 微基准 10-20× 提升、owner flush CPU 降 ~20-30%。然后再看 A（架构级，中等风险）：把 leaf page CRC 下放给 worker 算（`worker_leaf_chain::leaf_page_image` 生成时 inline 算完 CRC 填头），owner 只算 internal/root CRC；预期 owner CRC 工作量减 ~99%，overall owner flush CPU 降 60-70%。A 的契约改动需要同步到 `INC-046` design_doc 里"worker 返回 zero-extra-copy leaf carrier"那节（明确 CRC 是 worker 的职责不是 owner）。暂不做 C（磁盘格式改 CRC 到页尾，要动 superblock 兼容）、D (静态链 absl，收益不如 B) |
| INC-053 | `apps/inconel/tree/owner_scheduler.hh:63` 的 `tree_allocator::free_ranges: pump::core::local::queue<range_ref>{4096}` 作为退役 range 的缓冲，数据结构本身有三层设计问题，**与上层 retire_dispatch 是否已接通正交**，现在就可以独立改造：(1) **adjacency 不回吞 bump**：`recycle(r)`（L117-119）无脑 `try_enqueue`，即使 `r.base.lba + range_lbas == head.lba`（退役 range 正好贴在 `head` 下方）也照样占一条队列，不把它吞回成 `head.lba -= range_lbas`；`push_back_bump(r)`（L99-114）虽然做这件事，但前置条件硬约束 `r.base.lba + range_lbas == head.lba`，不邻就 panic，只能在 round 失败 rollback 按反向 LIFO 顺序用。(2) **连续 retire 不合并**：相邻多条退役 range 各占 N 条 queue entry，没有 extent coalesce。(3) **容量满 silent drop**：`try_enqueue` 返回 `false` 时 `recycle` 不处理，超过 4096 条直接丢弃，对应退役 range 的盘 LBA 永久 leak——不是内存问题（`local::queue{4096}` 固定 64 KB 预分配），是容量 / 正确性问题。当前即使 retire_dispatch 还没接上，rollback 路径 (`emit_finalize_merge_failure` L2656 → `rollback_allocated_ranges_from_merge` L2307) 已经被迫按反向 LIFO 遍历 `allocated_ranges`，就是这个数据结构强加的约束；做完改造 rollback 立刻受益。上层 retire_dispatch 真正把 retire bag 接到 `alloc.recycle(...)` 属于 `INC-022`（tree flush handle 整套，其 frontier_switch 子任务）的 scope，与本 issue 正交，不作为本 issue 前置。 | 2026-04-22 tree allocator / 8T 大盘 / recycle 累积 / adjacency 讨论顺出；代码点 `apps/inconel/tree/owner_scheduler.hh:61-119`（`tree_allocator` 定义） | `normal` | 把 `free_ranges` 换成按 `base.lba` 排序的 coalescing extent map（`absl::btree_map<uint64_t, uint32_t>`：key=base_lba，value=lba_count，每节点代表 N 条连续退役 range 合并出的一段）。`recycle(r)` 改三步：(a) 在 map 里找左/右邻并合并；(b) 合并后若新 extent 贴 head（`extent.end_lba == head.lba`），`head.lba -= extent.lba_count`，然后 cascade 检查是否还能再回吞下一条 extent；(c) 不贴 head 则插入 map。`allocate()` 优先从 map 取（倾向 last-fit：取最大 `base_lba` 的 extent，让 head 能稳定回退、跟 recycle 的 cascade rewind 协同），剩余再 bump。**`push_back_bump` 删除**——新 `recycle` 自带"贴 head 即 rewind"，失败 rollback 用 `for (r : allocated_ranges) alloc.recycle(r)` 任意顺序即可，`rollback_allocated_ranges_from_merge` 不再需要反向遍历。内存上每节点 ~28 B（btree_map 每节点摊销），节点数随碎片自然收敛，稳态下远小于退役 range 数；极端最坏（每隔一个 range 都 retire）时等同于退役 range 数，但此时现在的 4096-slot queue 也早满了，不比现状差。成本：recycle / allocate 从 O(1) 变 O(log n)，n = extent 数（典型 <几百），绝对 cycles 相比 NVMe I/O 可忽略。与 `INC-051`（value 整页空闲空间的 class-agnostic 统一表示）方向同源但范围不同：本 issue 限定在 tree 的 fixed-size range 层，结构简单单一，可独立落地；若日后决定 tree / value 整页空闲空间完全统一走 INC-051 的 global bitmap+summary，再把本 issue 的语义合入。 |

### 未来功能的基础，建议提前设计 + 实现

这些项现在不一定立刻“被用到”，但会冻结 format、runtime 边界或核心抽象。晚做通常会让后续大功能先搭在错误地基上。
`INC-031` / `INC-034` / `INC-036` 已分别由 step 016 / step 017 / step 018+019 收敛；当前剩余 open 项主要集中在“全局整页空闲空间表示 / 分配”这一层。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|

### 最好等 Tree 写侧 / Allocator / Reclaim 模块

这些项要么本身就是 tree 写侧，要么强依赖 tree allocator / reclaim 真实落地后才能做对。
`INC-046` 已在 2026-04-19 以 leaf-only worker + owner-only reverse/non-leaf cache 的 canonical path 收敛；本组当前剩余 open 项主要是更大范围的 tree/reclaim 基础设施。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-001 | tree multi-level descent 在 production 0 验证 | audit/tree.md F3 | `blocked` (tree_sched 写侧 + property test) | tree_sched 写侧落地后补 property test 强制 ≥3 层；mitigation 期加 depth assert + 模块 scope 注释 |
| INC-011 | `tree_manifest::has_root()` 用 `lba == 0` 当 sentinel，依赖隐式不变量 | audit/tree.md F10 | `blocked` (tree_sched 写侧 / page allocator) | 等 allocator 落地知道哪些 lba 合法后定方案（optional<paddr> 或显式 bool 字段） |
| INC-022 | tree 模块缺 `flush` handle 整套（spec RSM §4 + FF §3：tree_allocator / checkpoint_guard / fold / consolidation / frontier_switch / new manifest） | audit/tree.md F4（扩展） | `urgent` | 实现 tree_sched 写侧基础设施（allocator + checkpoint_guard + retire queues）+ flush handle；这是 INC-001 的真正解封路径 |
| INC-023 | tree 模块缺 `reclaim` handle（spec RSM §4.2：TRIM 旧 slot/range + 调用 value 的 batch reclaim） | audit/tree.md F4（扩展） | `urgent` | 实现 reclaim handle，收集 dead `value_ref` 并 batch dispatch 到 `value::reclaim_values(...)`；TRIM 顺序遵循 spec §6.9 |
| INC-024 | tree 模块缺 `update_superblock` handle（spec RSM §4.2：root-change flush 后异步更新 superblock + 推进 superblock_safe_lsn） | audit/tree.md F4（扩展） | `low` (Phase 1 不重启) | 等 superblock 持久化路径整体落地时实现 |
| INC-052 | `value::reclaim_values(...)` 对输入 `value_ref` 集合是 blind trust，若上层（未来 tree reclaim consumer / consolidation diff / tombstone GC / snapshot reader guard 等推导 dead set 的路径）误把仍 live 的 vr 判成 dead 投进来，value 侧在 `apps/inconel/value/scheduler.hh::apply_partial_reclaim`（L1474-1548）六条分支——dirty(L1480) / open(L1487) / allocatable(L1499) / cache(L1510) / hole(L1531) / 完全未追踪兜底(L1541-1547)——以及 `apply_whole_reclaim`（L1551-1588）**全部无 liveness 校验**，会无条件把对应 slot 标为可写空位（或整页进 trim_pending）；下次 acquire 同 class 命中该 hole 走 `dirty_hole_fill` prefill-read → 覆盖写该 slot → tree 里指向老 vr 的 live key 读到新 body，**silent data corruption**。全链 CRC / magic / `tree_manifest::resolve` invariant 全过，事后 TRIM 完成老页可能已物理擦除 → 法证抓手 = 0，只表现为"业务层某些 key 的值变了"。value 侧自行防御（扫 tree 活集 O(N) 且与 reclaim 同级代价）成本不对等，且无法覆盖"上层整套 dead 判定一起错"的协调错情形，因此**不该加防御代码**；真正的健壮性边界在上层推导逻辑 + spec trust boundary 登记 + 最小可观测 counter 上 | 2026-04-22 step 036 落地后 P0-2 review 顺出；与 `INC-023` tree reclaim handle 强耦合（调用方就是 retire consumer） | `normal` | 分两步统筹、**等 `INC-023` / retire consumer / consolidation / tombstone GC / snapshot reader pin 一整套模块成组落地时作为 v1 trust boundary package 统一处理**，不要先插入当前实现：(1) spec 层把"`reclaim_values` 的 caller 必须保证每条 vr 对 tree + outstanding read_handles 皆 dead"明示写进 `cross_doc_contracts §1 reclaim_values` precondition 与 `§6` 红线；`write_path_and_pipeline` / `flush_and_frontier_switch` / `read_api_and_pipeline` 里同步写清推导规则（flush loser 需 `gen.max_lsn < recovery_safe_lsn`、tombstone 物理删除需 `tombstone.data_ver < recovery_safe_lsn`、consolidation 保留集需出自同一 tree snapshot、snapshot reader 的 read_handle 必须 pin 到 release 后才允许对其可见 vr 做 reclaim）。(2) 代码层加最小可观测：`value_alloc_sched` 新增 atomic `reclaim_stats { reclaim_total_refs, partial_into_{dirty,open,allocatable,cache,hole,untracked}, whole_{into_dirty,clears_existing,already_pending}, dropped_freed_mask_zero }`，facade 暴露 inspector；稳态下 `partial_into_untracked` 应 ≈ 0，持续非零或 bump = caller 侧 dead set 推导出问题的一手信号，把 silent corruption 从"业务审计才发现"降级为"监控可报警 + 事后可归因"。**明确不加任何 liveness 校验防御代码**（成本与 reclaim 同级 + 无法覆盖协调错 + 给人"防住了"的虚假安全感） |

### 最好等 NVMe / BufferPool / Device Abstraction 模块

这些项的正确边界在 `apps/inconel/nvme/`、设备抽象和 DMA buffer ownership 上，提前做很容易再返工。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|

### 最好等 Front / WAL 模块

这些项和 front owner、WAL append 路径的真实接口一起定最稳妥。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-055 | 设计决策更新：memtable 不再保存 value body / `value_view` hot copy，只保存 durable `value_ref`。当前文档已改成该方向，但 `apps/inconel/core/memtable.hh` 与 `inconel_test_flush_e2e` harness 仍保留旧 carrier（`value_handle { durable, hot }`、`kv_arena` 同时存 key/value），应视为 front/memtable 模块未落地前的临时形态。正确写路径是 `value_alloc_sched.persist_put_values()` 先把 value object 写入 Value Area 并返回 `value_ref`，WAL 写 `PUT(value_ref)`，memtable 只插入 `{key, data_ver, value_ref}`；memtable hit 读路径拿到 `value_ref` 后走 `value_alloc_sched.read_value()` / `read_page_values()`，不得在 memtable 内复制或长期持有 value bytes。这样前台提交的 value body CPU copy 目标是 1 次：request/canonical body → DMA value frame。 | 2026-06-08 真实 NVMe 路径 review：用户明确质量/效率优先，不能用“读路径/生命周期简单”作为保留 memtable hot copy 的理由；`design_overview.md` / `runtime_state_machine.md` / `runtime_memory_and_cache.md` / `write_path_and_pipeline.md` 同步改为 no-value-in-memtable 方向 | `urgent` | 等 front/WAL/memtable 模块实现时成组修改：(1) `core::value_handle` 收敛为 `value_ref` 或只含 `durable`，删除 `value_view`；(2) `memtable_gen::kv_arena` 只保存 key bytes，`insert_memtable_entries` 不再 copy value body；(3) `lookup_memtable` 输出改为 `variant<value_ref, tombstone, miss>`，read pipeline 对 memtable value hit 调 `value::read_value(value_ref)`；(4) value 层优化 recently-written / memtable-visible value cache：FUA 完成后的 clean value page frame 尽量进入 `readonly_frame_cache` 或 dedicated write-through residency tier，并按 budget/epoch/pin 管理，使 memtable hit 后的 `read_value()` 尽量命中内存而不是读盘；(5) 更新 e2e harness，构造 sealed gen 时不再把 `op.value` copy 进 `kv_arena`。 |

### 最好等 Runtime Format / Recovery 模块

这些项要和“如何格式化盘面、如何恢复 superblock / recovered state”一起落地。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-035 | runtime/ 仍缺生产级 `format_disk()` 命令路径，spec ODF §7 定义的完整格式化流程（计算区域边界 + TRIM 整盘 + 写 superblock A/B + 后续恢复入口）尚未作为 runtime/recovery API 固化。当前 `inconel_test_flush_e2e` 只在真实 NVMe 上写 bootstrap superblock A/B，作为 destructive e2e harness 的前置格式化 | audit/runtime.md R7；2026-06-08 flush_e2e 已改为真实 NVMe bootstrap format，并在空闲盘 `0000:04:00.0` 上用本地 SPDK/DPDK `LD_LIBRARY_PATH=/home/null/work/kv/spdk/build/lib:/home/null/work/kv/spdk/dpdk/build/lib` 跑通 3 轮真实 NVMe write/flush/readback | `normal` | 把 e2e 内部 bootstrap format 提炼成生产 `format_disk()`：算 region 边界、TRIM 整盘、写 superblock A/B、必要 flush/fence，并与 INC-020 install_recovered_state 成组接入 runtime/recovery |

### 最好等 Value Reclaim / Recovery / Read Pipeline 模块

这些项属于 value 模块缺失的 handle 族，最好作为成组功能落地，而不是零散插入当前实现。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-020 | value 模块缺 `install_recovered_state` handle（boot recovery 入口，spec RSM §6.2） | audit/value.md V3 | `low` (Phase 1 dev 不重启从 0 init) | 等 recovery 路径整体落地时实现 |
| INC-021 | value 模块缺 `read_page_values` handle（MultiGet/Scan 批量读，spec RSM §6.5） | audit/value.md V3 | `low` (Phase 2 用) | 等 read pipeline + Scan/MultiGet 落地时实现 |

### 最好跟 Cache Concept 重构同批处理

这些项单独做收益有限，跟核心抽象一起收才不容易再次漂移。
`INC-038` 已由 step 018 跟 `INC-036` 同批收敛；当前本组暂无 open 项。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|

### 最好等 PUMP 框架 / Sender Contract 修正

这些项本质上不是 Inconel 业务逻辑 bug，而是当前 `pump/` sender contract 或实现边界不够清楚；Inconel 侧可以先绕开，但最终要回框架层拍板。

| ID | Issue | 来源 | Priority | 方向 |
|----|-------|------|----------|------|
| INC-041 | `PUMP` 的 `concurrent() >> reduce(...)` / `to_vector()` 对共享 accumulator 的 contract 不明确且当前实现不安全：`reduce` 回调直接原地改同一个 result，对 `vector<worker_tree_proposal>` 这类非线程安全容器会 data race；同时 `reduce_sender` 构造还把初值按拷贝路径放进 `result`，move-only accumulator 无法实例化 | 2026-04-16 `029_owner_closure` owner pipeline 调试；`pump/sender/reduce.hh` + `pump/sender/concurrent.hh` | `blocked` (pump/) | 在 `pump` 层明确并收紧 contract：要么禁止 `concurrent` 后直接接共享 `reduce`/`to_vector`，要么提供 per-branch local reduce + final combine 的安全 collector；同时修 `reduce_sender` 对 move-only 初值的构造语义并补文档。在框架修好前，Inconel 侧不要再用 `concurrent() >> reduce(...)` 收集 `worker_tree_proposal` 这类 move-only / 非线程安全结果 |

## Resolved

| ID | Issue | 来源 | 解决 |
|----|-------|------|------|
| INC-048 | `value::persist_values` 对单次 entries 跨度没有分块：内部 `on_persist_leader` 展开成 `as_stream(writes) >> concurrent() >> flat_map(nvme::write)`，用**无界** `concurrent()` 一次性把 N 个 `nvme->write(...)` 全部塞进 per-core NVMe scheduler queue。当 harness 单次传入 ~1M 个 `put_entry`（step 035 N=10^6 场景）时 value_alloc_sched 的 leader 批次可能直接把 NVMe per-core queue 打爆、触发 `FATAL: per_core::queue full` | 2026-04-20 step 035 Phase 1 `inconel_test_flush_e2e --num-keys 1000000` 首次暴露；harness 侧通过把 `persist_put_values` 拆成 1024 个 put 一批临时绕过 | 2026-06-12 已由 M07/046 收口：接口统一 rename 为 `value::persist_put_values`，`value_io_policy` 提供 write/read/trim bounded inflight，leader/prefill/trim 全部改为 bounded sender；`write_path::persist_put_values(core::batch_ctx&, NvmeProvider)` 增加 DELETE-only 短路、oversized 预检和 `round_failed` 错误映射。备注：`test_flush_e2e` 的 `kPersistChunkPuts = 1024` harness 兜底暂保留，待真盘 e2e 覆盖后撤。 |
| INC-002 | 旧内存 NVMe 路径硬耦合：tree lookup / runtime device 依赖非 DMA 内存与内存 device 类型，无法接真实 NVMe | audit/tree.md F5 + audit/runtime.md R3 | 2026-06-08 已完成：`nvme::runtime_device` / `runtime_scheduler` 固定为真实 SPDK/PUMP NVMe，tree/value/superblock I/O 走 segmented LBA DMA frame + `nvme::frame_io`，旧内存 NVMe 模块已删除，runtime builder 接真实 device/qpair/DMA pool |
| INC-016 | tree + value 两侧 cache buffer ownership 都是 workaround：tree 用 free_bufs_ + owned_bufs_ 析构隐式释放；value `~scheduler()` 手动 evict_one drain | audit/tree.md F15 + audit/value.md V9 | 2026-06-08 已完成当前生产路径：runtime tree/value cache 使用 segmented frame carrier 和 LBA-sized DMA page pool；旧 contiguous `page_frame` 只保留为 standalone cache policy compatibility carrier，不再是 Inconel runtime I/O 路径 |
| INC-051 | 当前整页空闲空间仍是 value 私有、按 class 分散的 `whole_pool: vector<paddr>` + `trim_pending_pages_` 模型：小规模下能工作，但对大盘和高 churn 不成立。问题有三层：一是内存成本按"已回收整页数 × `paddr`"线性增长，8 TiB / 4 KiB 量级不适合继续用逐页 `vector<paddr>` 表示；二是 whole-page reclaim、TRIM、重新分配被绑在 value 私有状态机里，导致后续 tree reclaim / recovery / 后台 trim 很难复用同一套空闲空间真相；三是当前 whole-free page 必须先进 `trim_pending_pages_`、等显式 drain 完成后才能回 `whole_pool`，把"逻辑释放"和"物理 trim"耦死。 | 2026-04-22 讨论 value reclaim / TRIM / 8T allocator 时确认 | 2026-04-25 已完成：按 [037_inc051_value_space_manager.md](plan/037_inc051_value_space_manager.md) 落地。manager 侧实现 `apps/inconel/value/space_manager.hh`：`global_free_extents`（`absl::btree_map<base_lba,len>`）作为 whole-free 唯一真相，sparse_partial_repr per group + `pages_by_largest_run` selection bucket，`allocate_batch` 三档 pressure mode（normal / reuse_pressure / hard_pressure）+ BFD planner + round-local fresh extents + tail partial 发布，`sync_alloc_floor` request fence + owner-local ack 协议（manager 侧已实现，tree 侧调用拆出 `INC-054`），cached_partial admission/selection（active_tail 优先 + smallest leftover + recency），`prepare_trim` / `complete_trim` withhold，`install_recovered_state` 从 live extents 重建 free 补集。disk-format 字段 `value_space_quantum_bytes` / `value_space_group_size_lbas` 加入 `format_profile` / `superblock` 并在 `profile_is_self_consistent` / `validate_build_inputs` 内 fail-fast。`value_alloc_sched` 整体重写：`value_allocator` / `whole_pool` / `hole_pages_` / `trim_pending_pages_` / `value_page_frame` / `value_page_source` / `value_alloc_result` / `per_device_value_state` 物理删除（`apps/inconel/value/allocator.hh` 整文件删除）；persist 路径 leader/follower batch + cached_partial pin/take + new_whole_page 零填 + nonresident_partial prefill；reclaim 路径走 manager `release_values` + readonly_cache → resident_partial 自动 promote（reclaim 命中 cached page）；TRIM 走 prepare/complete + withhold；recovery 入口 `install_recovered_value_space()` 已暴露但调用方留给 INC-031/INC-035。新增 standalone test `inconel_test_value_space_manager`；`inconel_test_flush_e2e` 编译通过。同时修掉接入过程顺出的三条 bug：(a) prefill UAF（leader 已 delete 但 round.items 仍持指针，由 round 改成只存 followers + 把 entries snapshot 进 round 解决）、(b) prefill 失败漏通知 follower（统一 `cb(persist_follower{false})` + delete）、(c) `prepare_trim` 空计划残留 `trim_inflight_` 条目（manager 侧 empty plan 不再 ++seq、不注册，并在 spec 里把 `trim_plan_id::raw == 0` 钉死为 idle 哨兵）。剩余未做但同源的 cross-core "tree 反向接管 value 释放低位空间" 协议（tree 侧调 `sync_alloc_floor(...)`）拆出为 `INC-054` urgent。提交：`ed0e8cb` (manager + disk format + standalone test) + `147a8c9` (wiring + bug fixes) |
| INC-046 | 当前 tree 写侧整体不符合 Shadow CoW spec（普通更新无条件 cascade 到 root，且 internal `child_base` 错写 slot paddr，导致跨 round lookup/flush correctness bug） | 2026-04-19 `INC-044` review 顺出 spec gap；`candidate_build.hh::initialize_cascade` / `owner_scheduler.hh::reformat_internal_node` / `lookup_scheduler.hh` / `tree_manifest.hh::resolve` | 2026-04-19 已完成：按 [034_inc046_shadow_cow_write_side_rewrite.md](/home/null/work/mine/nitro/ai_context/inconel/plan/034_inc046_shadow_cow_write_side_rewrite.md) 重写 canonical path，`lookup` 改成 leaf-only，worker 改成 leaf-only carrier，owner 独占 non-leaf cache / reverse / placement / manifest rebuild，删掉旧 `worker-local root` / `initialize_cascade()` / `run_merge()` 模型，`internal child_base` 固定写 child `range_base`。新增 `inconel_test_inc046_regression` 覆盖跨 round rewrite 与 compact-to-empty 保守表示；旧 `test_candidate_build` / `test_flush_carriers` 删除。后续结构收缩问题转入 `INC-047` |
| INC-003 | tree lookup sender 让 caller 自由传 sched 指针，没有内部路由解析 | audit/tree.md F7 | 2026-04-16 step 030 最终形态：公开 API 稳定为 `tree::lookup(keys, manifest)`（无 sched 指针）；sender 内部 `build_route_plan` 改用 `current_shard_partitions()->route(key)` 分组，fan-out 目标从 `tree_lookup_at(idx)` 换成 `tree_read_domain_at(idx)->lookup_sched`。与 INC-040 一并收敛 |
| INC-040 | front→tree_lookup 的路由 spec 和实现都写成 hash-based (`home_tree_lookup(front_owner)`)，与设计意图不符。正确意图是 key-range based：同一 leaf page 的所有 key 路到同一 read_domain，保证一张 page 在系统里只 cache 一次 | 用户澄清（2026-04-15）；当时写在 RSM §4.7、design_overview read 伪码、registry.hh L209-217 注释 stub、audit/tree.md F7；plan/027 Gap 3 讨论副产出 | 2026-04-16 step 030 最终形态：(1) routing carrier 从 `leaf_order.find_leaf_for_key(key) % K`（前一轮 INC-040 修过的 ordinal modulo）彻底换成**全局 `shard_partition_map`**：`current_shard_partitions()->route(key)` 一次二分拿 `shard_idx`，lookup / flush fold 两条路径**共用同一张 map**（`memtable_fold::build_key_partitions` 同步切换），保证 "同 key → 同 shard" 不变量。(2) 立起 `core::tree_read_domain<Cache>`，own `tree_lookup_sched` / `tree_worker_sched` + `node_cache` + routing snapshot；PUMP runtime tuple 折成单 read_domain 槽位；registry 只留 `tree_read_domains.list`，旧 `tree_lookup_scheds` / `tree_worker_scheds` / `tree_lookup_at` / `tree_worker_at` / `route_tree_lookup_for_key` 全删。(3) `inconel_test_tree_lookup` / `inconel_test_tree_lookup_multicore` / `inconel_test_runtime` 三个验收测试 pass；spec 同步覆盖 RSM §1/§4/§4.7/§4.8/§9.3/§10.3、design_overview §1.7/§1.8/§8.1/§14、read_api §4/§5/§5.4/§9.2、code_modules、INDEX；audit/tree.md F7 标记 resolved |
| INC-042 | Phase 9 空树 bootstrap flush 未闭：`tree/memtable_fold.hh:234` `build_key_partitions` 对 `!has_root() \|\| leaf_order.empty()` 直接返 `unsupported_shape_change`，导致 fresh-formatted disk 起第一次 flush 无法产出 tree；worker `initialize_worker` 与 owner `run_merge` 也没有 `has_root()==false` 分支 | 2026-04-19 step 033 harness `inconel_test_flush_e2e` 首次跑暴露（st=2, leaf_order.size=0, root_slot.lba=0） | 2026-04-19 解决：(1) `memtable_fold.hh::build_key_partitions` 拿掉 narrowing，空 base_manifest 也走 `shard_partition_map` 路由。(2) `candidate_build.hh` 新增 `_wb::build_leaves_from_sorted_keys_impl` + `_wb::initialize_worker_bootstrap`，空 `leaf_order` 时 worker 直接把 sorted key_groups chunk 成若干 fresh leaf page，单 `leaf_work` 带多 `built_leaves` 喂给 `finalize_root` 已有 wrap 逻辑；同时在 `process_flush_round` 开头加 `s.all_done` 早退，修复协程 driver 二次进入会把 moved-out `built_leaves` 再 wrap 一次导致 panic 的顺带问题。(3) `owner_scheduler.hh::run_merge` 按 `is_bootstrap=!has_root()` 分支：bootstrap 用新的 `make_merge_context_bootstrap`（跳过 `index_root_group`）+ `combine_worker_roots_for_bootstrap`（按 `first_key_of_worker_root` 排序 worker proposal、`build_internal_pages_owner` 包成 fresh internal 层）；Phase 3/4 post-order walk + `build_leaf_order_full` + `build_reverse_topology_full` + `rebuild_slot_map` 原样复用（bootstrap 下没有 paddr-ref 子节点，所有复用分支都只走 unique_ptr 路径）。`is_root_change` 对 bootstrap 返 true → 进 root-change 流程异步更新 superblock。(4) `tree/sender.hh::tree_local_flush` 在 `continue_after_finalize_merge()` 后追加 `then` 调 `rebuild_and_publish_shard_partitions`，基于 `tree_flush_result.new_manifest->leaf_order` + `tree_read_domain_count()` 重建 `shard_partition_map` 并 `rt::publish_shard_partitions` 替换全局 + 每个 read_domain 的 snapshot，production 路径不再依赖 harness 预先手动 publish。全部 15 个 `inconel_test_*` 及 `inconel_test_flush_e2e` pass |
| INC-043 | 多轮 flush 的 owner merge 在 round 2 `try_get_old_page_bytes` 段错误（INC-042 之后 harness 扩成 3 轮才首次覆盖多 contrib 路径）：两层相关缺陷同时触发——(a) `merge_context::group_storage` 是 `std::vector<proposal_group_view>`，`add_group_view` 每次 `push_back` + 立即捕获 `&group_storage.back()` 存进 `contrib_index`，vector 扩容会把先前存的所有 `proposal_group_view*` 全部 dangling，`try_get_old_page_bytes` 解引用时崩；(b) 修完段错误后 `merge_internal_old_paddr` 多 contrib 路径 `materialize_substitution(raw_group)` 把 `*iv.raw_view`（整个 worker 的 proposal_group_view = 整棵 worker root internal）原样 clone 进 new_children，丢掉了 [j, k) 切片信息，导致 combined_root 变 "old_root 合并层 × 3 worker 全量视图 + 包一层"，round 2 的 leaf_order 出现 3× 重复 leaves 和非首位 fence_lower=空的错误（破坏后续 `build_initial_shard_partition_map` 不变量）；bootstrap 路径 `make_merge_context_bootstrap` 跳过 `index_root_group`，`group_storage` 全程空，掩盖了 (a)；单 contrib 路径短路用整个 worker root 刚好也对，掩盖了 (b) | 2026-04-19 step 033 harness 扩成 3 轮 1000+400+400 ops (含 overwrite/tombstone/tail 扩展) 首次暴露：round 2 进 `tree_local_flush` 后 tree_sched.advance 线程 SIGSEGV | 2026-04-19 解决：(a) 把 `merge_context::group_storage` 换成 `std::deque<proposal_group_view>`（deque 对 `push_back` 保证 references/pointers 稳定，标准条文），`contrib_index` 存进去的指针不再随 capacity 翻倍失效；`<deque>` 加到 include 清单。(b) `substitution_iv` 在 `raw_group` 分支新增 `raw_nodes: vec<const mem_tree_node*>` + `raw_sibling_seps: vec<string_view>`，在 per_contrib iv 构造循环里从 `flat_children[j..k)` / `flat_separators[j..k-1)` 直接切片保存；`materialize_substitution(raw_group)` 改成 `clone_node(n)` 每个 `raw_nodes` 而非 `clone_group_view(*raw_view)`，原 `raw_view` 仅留作 dedup-check 的 source identity。另外在 `build_leaf_order_full` 加 CLAUDE.md 约束 A 的 fail-fast：非首位 leaf 若 `lowers[i]` 为空直接 `panic_inconsistency` 并在消息里指明两种已知触发路径（被挤走的 base position-0 leaf / 未剪掉的空 new leaf），标为 INC-043 follow-up 的 known shape-specific 限制。验收：`inconel_test_flush_e2e` 3 轮全 st=0 + readback OK for 51 samples (43 value / 8 tombstone 相加等于 sample 总数)，全部 15 个 `inconel_test_*` 无回归。跟随 issue：Phase 9 review §11 的 `run_merge` 协程帧 `retire_slot` / `retire_range` lambda `-Wsubobject-linkage` warning 未顺手清（release LTO 层诊断，不影响 correctness），留作后续清理 |
| INC-004 | tree + value 检测到 corruption 时走 silent fallback 或 throw 而不是 panic：tree::process_entries 把 CRC/magic/zero 失败映射成 `lookup_absent`；value::scheduler `handle_fill` / `serve_hit_or_fail` 走 cb/fail dual callback 抛异常进 PUMP exception path | audit/tree.md F1 + audit/value.md V1 + V7 | step 009 已完成：新增 `core/panic.hh::panic_inconsistency`，tree page corruption 与 value decode corruption 统一改为 fail-fast panic，并携带 `tree_page_status` / `value_decode_status` 具体 reason |
| INC-005 | `tree_manifest::resolve` 用 assert，Release 退化 UB | audit/tree.md F2 | step 009 已完成：`tree_manifest::resolve()` 在 miss 时改为 `panic_inconsistency(...)`，不再依赖 Release 会消失的 `assert` |
| INC-007 | `tree_manifest` 模块归属 core/ vs tree/ 不明，spec 没明说 | audit/tree.md F6 | step 011 已完成：在 `core/tree_manifest.hh` 文件头明确记录归属决策，拍板继续留在 `core/`，不做文件迁移 |
| INC-008 | spec ODF §4.2 的 `internal_record` 没有 import 到 `format/tree_page.hh`，layout 散在 page_builder/reader 4 处散文 memcpy（leaf record 是正确范本） | audit/tree.md F8 | step 009 已完成：`format/tree_page.hh` 新增 `internal_record` / `internal_record_size()` / key-child helper，`tree/page_builder.hh` 与 `tree/page_reader.hh` 全部收敛到这些 helper |
| INC-009 | `loading_pages_` / `slot_map` 用 std::unordered_*，spec 用 flat_hash_map | audit/tree.md F9 | step 008 已完成：`tree::lookup_scheduler::loading_pages_` → `absl::flat_hash_set`，`core::tree_manifest::slot_map` → `absl::flat_hash_map` |
| INC-010 | spec 用 abseil 容器词汇（flat_hash_map/set、btree_map、small_vector），代码用 std::* | spec 全文 | step 008 已完成当前已实现代码的容器对齐：shared page_cache `index_`、value `classes_` / `class_sizes_storage_` / `inflight_rounds_` 已切到 Abseil；future 模块首次落地时直接按 spec 选型，不再作为当前 open mismatch issue |
| INC-012 | leaf/internal page reader 全 linear scan，热路径每页 ~134/185 次比较 | audit/tree.md F11 | step 014 已完成：tree page layout 冻结为 `tree_slot_header + full slot directory + payload`，builder finalize materialize directory，reader 改成 directory + binary search，`rightmost_child()` 改 O(1) 取尾部 child，并新增 `inconel_test_tree_page_format` 锁定容量边界与 CRC/offset 语义 |
| INC-013 | `lookup_scheduler::handle` 的 `if (s.first_call)` 分支 + `lookup_state::first_call` 字段是 dead code（make_lookup_state 已处理空 manifest，coroutine 短路掉了 handle 入口） | audit/tree.md F12 | step 011 已完成：删除 `lookup_state::first_call` 字段和 `handle()` 中对应的 dead-code 分支，空 manifest 继续由 `make_lookup_state()` 初始化完成态 |
| INC-014 | tree (`lookup_scheduler` / `lookup_scheduler_base`) 和 value (`scheduler` / `scheduler_base`) 都用裸命名，跟 spec 名字 `tree_lookup_sched` / `value_alloc_sched` 不一致；user 报告 context 大时 AI 曾混淆这类裸命名 | audit/tree.md F13 + audit/value.md V4 | step 011 已完成：tree/value scheduler 类型统一 rename 到 spec 名字，并同步更新 sender / registry / runtime / 测试引用，不保留 compatibility alias |
| INC-015 | `tree/lookup.hh::decision_need_cache` variant 分支永不产生永不专门处理（fall-through 等同 done），暴露在 sender 类型上易被新 scheduler 抄成"预留 variant" pattern | audit/tree.md F14 | step 009 已完成：删除 `decision_need_cache`，`batch_decision` 收敛为 `variant<decision_done, decision_need_read>`，`tree/sender.hh` 的 visit 分发同步简化 |
| INC-017 | `value::handle_persist` rollback 时 fresh_bump / whole_page source 的 LBA 永久泄漏（silent state degradation），且 out-of-space 走 fail() 给 caller 而不是 panic（v1 没 reclaim 撞底不可恢复） | audit/value.md V2 | step 010 已完成：allocator 增加 `push_back_bump(...)`，rollback 逆序归还 `fresh_bump` / `whole_page` / `writable` 三类页；out-of-space 改为 `panic_inconsistency(...)` |
| INC-018 | value 模块缺 `freed_slots` handle（sub-LBA slot 回收，spec RSM §6.7） | audit/value.md V3 | step 036 已完成：公开接口收敛为 `value::reclaim_values(std::span<const value_ref>)`，value owner 内部按 `{class_idx, page_base}` 聚合 sub-LBA reclaim，并补齐 `hole_pages_` + `dirty_pages_` + `deferred_freed_` 状态机；readonly cache / resident frame / metadata-only hole page 都能消费 batch reclaim，详见 [036_value_reclaim_sender_support.md](/home/null/work/mine/nitro/ai_context/inconel/plan/036_value_reclaim_sender_support.md) |
| INC-019 | value 模块缺 `recycle_whole` handle（整页回收，spec RSM §6.8） | audit/value.md V3 | step 036 已完成：whole-page reclaim 也收进 `value::reclaim_values(...)`；owner 在 batch 内识别 single-slot class，并把 all-free page 先放入 `trim_pending_pages_`，等待 batched TRIM 完成后再归还 whole-page pool；persist finalize 后若 deferred reclaim 让页变全空，也会走同一条 trim drain，详见 [036_value_reclaim_sender_support.md](/home/null/work/mine/nitro/ai_context/inconel/plan/036_value_reclaim_sender_support.md) |
| INC-025 | value `writable_pages_` 是对 spec `hole_pages` + `open_frames` 双概念的简化（合成单一 per-class queue），但 spec ↔ 代码对应关系没文档化 | audit/value.md V6 | step 010 先补明旧 `writable_pages_` 简化与 spec 的对应关系；step 019 进一步删除 `page_data` / `writable_pages_` 主路径，改成 `value_page_frame` + `open_frames_` / `allocatable_frames_`，把 dirty active resident frame 与 clean allocatable resident frame 拆开 |
| INC-026 | `value::handle_persist` 用 `goto round_failed` + 嵌套循环 + 复杂错误传播，user 报告读不懂、无法有效 review 正确性 | audit/value.md V8 | step 010 已完成：`handle_persist` 拆成 `collect_round_items → build_round → finalize_round_writes → publish_round` 四阶段，去掉 `goto`，并改为显式 `persist_entry_status` 分流 |
| INC-027 | `value::scheduler` 的 `class_sizes_storage_` + `class_sizes_view_` 双字段冗余（vector 可直接 decay 到 span，view 字段没必要） | audit/value.md V10 | step 010 已完成：删除 `class_sizes_view_` 成员，改成按需从 `class_sizes_storage_` 构造临时 `span` |
| INC-028 | `value::handle_persist` 的 leader-follower 合并把整个 persist_q_ 吃光，单 round 无上限 → leader latency 不可控（特别影响 perf tuning 的 tail latency） | audit/value.md V14 | step 010 已完成：`handle_persist` 增加私有常量 `kMaxFollowersPerRound = 64`，单轮只合并 leader + 64 followers，剩余请求留给下一轮 `advance()` |
| INC-029 | tree + value 的 `advance()` 用 `.drain()` 把单 queue 吃光，多 queue 之间无 fairness 保证，单次 advance 延迟不可控（一个 queue 满了会饿死其他 queue 的处理） | audit/value.md V14 + tree 同源 | step 012 已完成：tree `cache/lookup` 与 value `finalize/persist/read/fill` 全部改成 bounded per-queue loop，保留原队列顺序与 `advance()` 返回契约，不再单轮吃空整条 queue |
| INC-030 | format/ 缺 WAL 三个 POD type：`wal_segment_header` (ODF §3.2, 26B) / `wal_entry_header` (ODF §3.3, 25B + 编解码) / `wal_sealed_trailer` (ODF §3.4, 33B)，front_sched / batch PUT 路径需要 | audit/format.md F2-F4 | step 015 已完成：新增 `format/wal.hh`，落地 3 个 packed POD、header/trailer CRC + inspect helper、PUT/DELETE entry size/encode/decode helper 与 reason-aware status；同步 ODF §3.3/§3.4 命名；新增 `inconel_test_wal_format` 锁定 golden layout、CRC 与 failure status 分类 |
| INC-031 | format/ 缺 `superblock` POD（ODF §2.2 定义 ~120B 的 packed struct） | audit/format.md F1 | step 016 已完成：新增 `format/superblock.hh`，落地 packed `superblock` POD、CRC/status helper 与 A/B 选择 helper；新增 `inconel_test_superblock_format` 锁定 layout、CRC 分类和 same-generation conflict 语义 |
| INC-032 | `format/crc.hh::crc32c` 用 raw SSE4.2 intrinsics，缺标准 CRC-32C 的 init/xor conditioning，跟外部工具（btrfs/ext4/iSCSI 等）不匹配；spec ODF §1.3 含糊 | audit/format.md F5 | step 009 已完成：删除 `format/crc.hh`，`tree_page` / `value_object` 调用点切到 `absl::ComputeCrc32c`，并在 ODF §1.3 明确标准 CRC-32C + init/xor conditioning 语义 |
| INC-033 | `build_runtime` + `start.hh::run_with` 一连串 raw `new` 没 RAII，`start()` 抛或某个 scheduler 构造抛 → 已分配的 rt + scheduler 全 leak | audit/runtime.md R1 | step 011 已完成：`runtime::run_with()` 统一 catch `std::exception` / unknown exception 后直接 `panic_inconsistency(...)`，把 init failure 定义为 process-fatal，由 OS 回收 leaked 资源 |
| INC-034 | `build_options` 把 4 个 disk format 字段（`value_class_sizes` / `lba_size` / `value_data_area_base` / `value_data_area_end`）暴露为 runtime config，但它们是 disk format 决定，一旦写数据就锁死不能改；当前还有"传空 → silent disable" 等 fail 路径 | audit/runtime.md R2 | step 017 已完成：新增 `format/format_profile.hh` 作为 bootstrap disk-format 单一来源，从 `build_options` / `start_options` 移除 4 个 disk-format 字段；标准 `build_runtime()` 总是构造 value scheduler，并对 device/profile 不匹配做 build 阶段 fail-fast |
| INC-036 | `cache_concept`（page_cache.hh）value 类型是 raw `char*`，缺 `pin_count` 概念、缺 frame state machine（dirty_append/dirty_hole_fill/writeback_inflight/clean_readonly），跟 spec RMC §6.1 + §11 期望的 `lru_or_clock<frame_id, page_frame*>` + frame state 不符；与 INC-016 (buffer ownership / DMA 替换) 是正交两层 | audit/core.md C1 | step 018 已完成 readonly frame cache core：新增 `memory/frame.hh` 的 `frame_id` / `frame_state` / `page_frame` / `frame_pin`；`cache_concept` 从 raw `char*` / `get()` 迁到 `frame_id -> page_frame*` + `pin()` / `put(page_frame*)` / `drain_one()`；clock/slru 改为 pin-aware eviction，same-key replacement 也拒绝替换 pinned old frame；tree/value readonly cache 已迁到 `page_frame*`。step 019 完成 value resident frame state：新增 `value_page_frame`，value write/read 路径改为 `open_frames_`（dirty / writeback_inflight active frame）+ `allocatable_frames_`（clean_allocatable partial frame）+ readonly cache（clean_readonly full page）的显式状态机 |
| INC-037 | `slru_cache::evict_one` 从 protected 段 evict 时不重置 `in_protected`，`free_node` 也不重置；recycled node 带 stale `in_protected==true` 进 free list，下一次 alloc_node + link_probation_head 后 get() 会走错 `if (n.in_protected)` 分支调 `unlink_protected` 操作 probation 列表的节点 → 破坏 protected 列表指针 + 错误递减 prot_size_；当前不可达（evict_one 仅 teardown 用），但 latent + AI 抄走会扩散 | audit/core.md C2 | step 009 已完成：`slru_cache::free_node()` 清 `in_protected=false`，`alloc_node()` 增加 `assert(!in_protected)`，把 stale state 与不变量一起收紧 |
| INC-038 | `cache_concept::evict_one` 命名跟语义漂移：clock_cache 走 `index_.begin()` 非确定性（不按 clock policy），slru_cache 走 probation tail 后 protected tail（LRU 序），page_cache.hh 注释说 "teardown drain" 但名字像 "按 policy evict 一个"；concept 没规范，AI 给新 cache 实现 evict_one 时会两边各自漂 | audit/core.md C3 | step 018 已完成：`evict_one` 全面 rename 为 `drain_one`（cache_concept + clock_cache + slru_cache + tree/value use site），`page_cache.hh` 明确 `drain_one()` 是 teardown-only drain，不承诺 policy victim；runtime eviction 语义只保留在 `put()` 内部 |
| INC-039 | tree lookup inflight 仍是 `loading_pages_ + waiters_head_` 的简化模型，没对齐 spec RSM §4.7 的 `inflight_reads` single-flight 结构 | audit/tree.md F9 + design_doc/runtime_state_machine.md §4.7 | step 013 已完成：tree lookup 的 inflight bookkeeping 改成按 `paddr` 聚合的 `inflight_reads_` single-flight map，用 `wait_gen + wake_enqueued` 处理多页等待、旧注册失效和重复唤醒，不再扫描全局 waiter 链 |
